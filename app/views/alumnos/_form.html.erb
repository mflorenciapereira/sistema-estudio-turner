<%= simple_form_for(alumno) do |f| %>
	<%= f.input :nombre %>
	<%= f.input :apellido %>
	<%= f.input :tipoDocumento %>
	<%= f.input :documento %>
	<%= f.input :email %>
	
	<%= f.button :submit, class: "btn-primary" %>
<% end %>